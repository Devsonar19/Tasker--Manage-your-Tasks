import 'package:flutter/material.dart';

class TaskBottom extends StatelessWidget {
  const TaskBottom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(child: Text('New Tasks')),
    );
  }
}
